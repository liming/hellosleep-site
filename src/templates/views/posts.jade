extends ../layouts/page

include ../mixins/post

block content
  .posts-list
    each post in posts.results
      if section == 'tip'
        +content-tip(post)
      else if section == 'question'
        +content-post(post)