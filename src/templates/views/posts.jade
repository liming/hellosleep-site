extends ../layouts/page

include ../mixins/post

block content
  .card-columns.posts-list
    each post in posts.results
      if section == 'tip'
        +content-tip(post)
      else
        +brief-post(post)

  +pagination(posts)